apiVersion: v1
kind: Service
metadata:
  name: mempalace
  namespace: mempalace
  labels:
    app: mempalace
    component: server
spec:
  type: ClusterIP
  selector:
    app: mempalace
    component: server
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
